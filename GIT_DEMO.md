# Git Demo - Git Commands I Used

Below are the Git commands I used to contribute to this repository (if I used the terminal):

## 1. Clone the repository
`git copy` `https://github.com/mmssouza1/dataset_titanic.git`

## 2. Create a new branch
`git check-in features` `git -b /visualization`

## 3. Create a directory image and add images
`image` `mkdir`
# Then copy the images to the `image` directory

## 4. Create the report file `my_contribution.md`
`tap on my_contribution.md`
# Compose the report content

## 5. Commit the changes
`git add .

git commit -m "Add visualizations and reporting"

## 6. Push to GitHub
git push native features/visualizations

## 7. Create a pull request
# Execute on GitHub (this is the step I'm taking)
